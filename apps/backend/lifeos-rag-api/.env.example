# Backend Environment Variables

# --- LLM Provider Selection ---
# Options: "local" or "openrouter"
LLM_PROVIDER=local

# --- Local SLM Configuration (if LLM_PROVIDER=local) ---
# Default Ollama endpoint
LOCAL_SLM_URL=http://localhost:11434/v1
# Model name to request (must be pulled in Ollama, e.g., 'ollama pull llama3:8b')
LOCAL_SLM_MODEL=llama3:8b

# --- OpenRouter Configuration (if LLM_PROVIDER=openrouter) ---
OPENROUTER_API_KEY=your_openrouter_key_here

# --- Components ---
# URL for the Qdrant vector database
QDRANT_URL=http://qdrant:6333
# Neo4j Graph Database Credentials (if used - ArangoDB is preferred for LifeOS)
# Use "neo4j" hostname for within docker network, "localhost" for host access
NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password

# ArangoDB Graph Database Credentials (preferred for LifeOS)
ARANGODB_HOST=http://arangodb:8529
ARANGODB_DB_NAME=lifeos_graph
ARANGODB_USER=root
ARANGODB_PASSWORD=your_arangodb_root_password

# SQLite Database Paths
# Path to the SQLite database for the event log
SQLITE_EVENT_LOG_DB_PATH=./data/event_log.db
# Path to the SQLite database for read models (derived state)
SQLITE_READ_MODELS_DB_PATH=./data/read_models.db

# --- Authentication ---
# Secret key for JWT token encoding (generate a strong, random key)
SECRET_KEY=your_super_secret_jwt_key
# Algorithm for JWT token encoding
ALGORITHM=HS256
# Access token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- OpenAI (Legacy/Fallback) ---
# Used if not using the custom provider logic, or by some Llamaindex defaults if not careful.
OPENAI_API_KEY=dummy
